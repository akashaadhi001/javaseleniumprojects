# Workspace Instructions

- Keep the Maven project structure standard for Java.
- Prefer JUnit 5 for automated tests.
- Use Selenium Java dependencies from `pom.xml`.
- Verify the project with `mvn test` after changes.
